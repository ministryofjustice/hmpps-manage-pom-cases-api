server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

hmpps-auth:
  url: "${HMPPS_AUTH_URL:http://localhost:8090/auth}"

# example client configuration for calling out to other services
# TODO: Remove / replace this configuration
example-api:
  url: "http://localhost:8091"
  client:
    id: "example-api-client"
    secret: "example-api-client-secret"

hmpps-prisoner-search-api:
  url: "http://localhost:8080"
  client:
    id: "prisoner-search-api-client"
    secret: "prisoner-search-api-client-secret"

spring:
  config:
    import: optional:file:.env.test[.properties]
  datasource:
    url: 'jdbc:h2:mem:hmpps-manage-pom-cases-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1'
    username: manage-pom-cases
    password: dummy-password
